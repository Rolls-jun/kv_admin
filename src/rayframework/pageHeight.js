// 页面自适应高度
export function getPageHeight() {
  let val = window.innerHeight - 100;
  return val;
}
// 表格自适应高度
export function tableHeight416() {
  let val = window.innerHeight - 416;
  return val;
}
// 表格自适应高度
export function tableHeight460() {
  let val = window.innerHeight - 460;
  return val;
}
// 表格自适应高度
export function tableHeight430() {
  let val = window.innerHeight - 430;
  return val;
}
// 表格自适应高度
export function tableHeight400() {
  let val = window.innerHeight - 400;
  return val;
}
